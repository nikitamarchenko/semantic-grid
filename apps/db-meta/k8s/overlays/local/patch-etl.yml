apiVersion: batch/v1
kind: Job
metadata:
  name: dbmeta-etl
  namespace: prod
spec:
  template:
    metadata:
      labels:
        app: dbmeta-etl
    spec:
      containers:
        - name: dbmeta-etl
          image: 'dbmeta:3.20'